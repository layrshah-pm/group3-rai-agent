# RAI Compliance Agent — Dependencies
# Install with: pip install -r requirements.txt

# ── Core graph framework ──────────────────────────────────────────
langgraph>=1.1.0
langgraph-checkpoint-sqlite>=3.0.0
langchain>=0.3.0
langchain-core>=0.3.0

# ── LLM providers (choose one, both work) ────────────────────────
langchain-ollama>=0.2.0       # Ollama local models (gemma)

# ── PII Detection (Node: pii_agent) ──────────────────────────────
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
# After install, also run:
#   python -m spacy download en_core_web_lg

# ── Fairness / Bias metrics (Node: bias_agent) ───────────────────
fairlearn>=0.10.0
aif360>=0.6.0

# ── Explainability (Node: bias_agent / explainability) ───────────
shap>=0.44.0

# ── ML / Data (demo model training) ──────────────────────────────
scikit-learn>=1.4.0
pandas>=2.0.0
numpy>=1.26.0

# ── Vector DB / Embeddings (Node: policy_agent RAG) ──────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0
# torch is NOT a direct dependency — sentence-transformers pulls it in transitively

# ── Output validation ─────────────────────────────────────────────
pydantic>=2.0.0

# ── UI ────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.20.0

# ── Utilities ────────────────────────────────────────────────────
python-dotenv>=1.0.0          # for .env API key management
msgpack>=1.0.0                # langgraph-checkpoint-sqlite uses msgpack serialisation
pymupdf>=1.24.0               # PDF text extraction (file upload feature)
python-docx>=1.1.0            # Word document text extraction (file upload feature)
